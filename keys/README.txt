本目录存放本项目的固定 APK 签名密钥。

- 首次运行 GitHub Actions 时会自动生成 keys/release.jks 并尝试提交回仓库。
- 密钥口令（默认，可在工作流 build.yml 的 KS_PASS 修改）：
  vmos-hider-2024
- 用途：APK 签名必须固定，否则每次重新编译后都无法覆盖安装（需先卸载，
  会清掉已配置好的数据）。本仓库面向个人自用，密钥入库可接受；
  若仓库被公开，请立即重新生成密钥并修改口令。
